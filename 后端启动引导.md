# WayFare 后端启动引导

> 适用目录：`D:\Desktop\Meta AI\WayFare`  
> 当前目标：在本机调试 **Go 后端** + **Python 后端**，并完成 **PostgreSQL** 首次初始化

---

## 1. 我先帮你读过当前项目后的结论

当前仓库里和后端直接相关的目录是：

- `wayfare_backend/`：Go 网关后端
- `wayfare_ai_backend/`：Python AI 后端
- `src/`：前端 Astro 页面

当前代码的真实启动关系是：

1. **Go 后端是主入口**
2. Go 后端启动后，会**自动拉起 Python 后端**
3. Go 和 Python **共用同一个 PostgreSQL**
4. Go 对外暴露 HTTP 接口：`http://localhost:8080`

也就是说，**正常联调时，你主要启动 Go 后端即可**。

---

## 2. 当前仓库里已经确认到的关键点

### Go 后端

- 入口文件：`wayfare_backend/main.go`
- 监听端口：`8080`
- HTTP 接口：
  - `POST /upload`
  - `POST /chat`
- 数据库连接写死在 `main.go`：

```txt
postgresql://luckdd:123456@localhost:5432/wayfare_dbz
```

### Python 后端

- 入口文件：`wayfare_ai_backend/ipc_main.py`
- **它不是 HTTP 服务**
- 它是一个通过 **stdin/stdout JSON-RPC** 和 Go 通信的 sidecar 进程
- Python 的 `.env` 当前内容：

```env
LLM_API_KEY=
DB_DSN=postgresql://luckdd:123456@localhost:5432/wayfare_dbz
```

### PostgreSQL

Python 侧会在启动时尝试：

- 创建 `vector` 扩展
- 创建知识库相关表

所以这个项目不仅需要 PostgreSQL，还**很可能需要 pgvector 扩展**。

---

## 3. 当前本机已经发现的几个阻塞点

这是我根据当前代码和本地运行结果确认到的：

### 3.1 Python 当前缺少 `PyMuPDF`

`wayfare_ai_backend/document_parser.py` 里直接 `import fitz`，但当前虚拟环境里没有这个包。  
如果不补上，`ipc_main.py` 一启动就会报：

```txt
ModuleNotFoundError: No module named 'fitz'
```

### 3.2 Go 版本偏低

当前本机：

- Python：`3.13.5`
- Go：`1.24.4`
- Node：`22.14.0`

但 `wayfare_backend/go.mod` 里写的是：

```txt
go 1.26.1
```

所以直接 `go run .` 时，会尝试下载 `go1.26.1` 工具链。  
**建议你本机先升级 Go 到 `>= 1.26.1`**。

### 3.3 `psql` 当前不在 PATH

PostgreSQL 虽然你已经安装了，但当前终端里：

```txt
psql --version
```

是找不到命令的。  
所以你需要：

- 要么把 PostgreSQL 的 `bin` 目录加入 PATH
- 要么直接用完整路径执行 `psql.exe`
- 要么用 pgAdmin 打开查询窗口执行 SQL

### 3.4 `LLM_API_KEY` 目前是空的

这会影响：

- PDF 向量化
- RAG 检索
- 聊天生成

而且当前代码里 embedding 失败时会回退成 **1536 维零向量**，但数据库字段是 **`vector(1024)`**。  
所以 **如果没填 API Key，上传/向量入库很可能直接失败**。

---

## 4. 推荐的首次启动顺序

建议按下面顺序做：

1. 启动 PostgreSQL 服务
2. 创建数据库用户和数据库
3. 安装/启用 `pgvector`
4. 补齐 Python 缺失依赖
5. 配置 `LLM_API_KEY`
6. 启动 Go 后端
7. 让 Go 自动拉起 Python sidecar

---

## 5. 第一步：初始化 PostgreSQL

### 5.1 先确保 PostgreSQL 服务已启动

你可以在 Windows 服务里确认 PostgreSQL 服务已启动。  
如果你习惯命令行，也可以在管理员 PowerShell 里检查服务状态。

### 5.2 打开 SQL 工具

任选一种：

- `pgAdmin`
- 或完整路径运行 `psql.exe`

典型路径类似：

```powershell
"C:\Program Files\PostgreSQL\17\bin\psql.exe" -U postgres
```

> 把 `17` 改成你的实际版本目录。

### 5.3 执行下面这段初始化 SQL

```sql
-- 1) 创建项目使用的用户
CREATE USER luckdd WITH PASSWORD '123456';

-- 2) 创建数据库
CREATE DATABASE wayfare_dbz OWNER luckdd;

-- 3) 连接到 wayfare_dbz 后再执行下面两句
CREATE EXTENSION IF NOT EXISTS vector;
CREATE EXTENSION IF NOT EXISTS pgcrypto;
```

> `pgcrypto` 这句是为了避免后面如果出现 `gen_random_uuid()` 相关报错。

### 5.4 如果 `CREATE EXTENSION vector;` 报错

说明 **pgvector 还没装到 PostgreSQL**。

你需要先安装与你 PostgreSQL 版本匹配的 `pgvector`，然后再执行：

```sql
CREATE EXTENSION IF NOT EXISTS vector;
```

---

## 6. 第二步：配置 Python 后端环境变量

文件位置：

```txt
wayfare_ai_backend/.env
```

建议改成：

```env
LLM_API_KEY=你的真实大模型APIKey
DB_DSN=postgresql://luckdd:123456@localhost:5432/wayfare_dbz
```

### 注意

- Python 用的是这个 `.env`
- Go **没有读 `.env`**
- Go 的数据库 DSN 目前写死在 `wayfare_backend/main.go`

所以如果你想换数据库账号、密码或库名，必须同时改：

- `wayfare_ai_backend/.env`
- `wayfare_backend/main.go`

---

## 7. 第三步：补齐 Python 依赖

进入 Python 后端目录：

```powershell
cd D:\Desktop\Meta AI\WayFare\wayfare_ai_backend
```

如果想继续使用仓库里现成的 `.venv`，执行：

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -m pip install PyMuPDF
```

> `PyMuPDF` 当前没有写在 `requirements.txt` 里，但项目代码实际依赖它。

如果你想重建虚拟环境：

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -m pip install PyMuPDF
```

---

## 8. 第四步：启动 Go 后端（推荐联调入口）

### 8.1 进入 Go 后端目录

```powershell
cd D:\Desktop\Meta AI\WayFare\wayfare_backend
```

### 8.2 启动

```powershell
go run .
```

### 8.3 成功后你应该看到的现象

- Go 连接 PostgreSQL
- Go 自动建业务表
- Go 自动启动 Python sidecar
- Go 在 `8080` 端口监听

---

## 9. 为什么一定要在 `wayfare_backend` 目录里启动 Go

因为 `ipc.go` 里使用的是**相对路径**去找 Python：

- `..\wayfare_ai_backend\.venv\Scripts\python.exe`
- `..\wayfare_ai_backend\ipc_main.py`

所以请用下面这种方式：

```powershell
cd D:\Desktop\Meta AI\WayFare\wayfare_backend
go run .
```

**不要**直接在根目录执行：

```powershell
go run ./wayfare_backend
```

否则 Python sidecar 的相对路径很容易错。

---

## 10. 如果你想单独调试 Python 后端

Python 当前不是 FastAPI HTTP 服务，所以：

- **不要用** `uvicorn main:app`
- 正确入口是：

```powershell
cd D:\Desktop\Meta AI\WayFare\wayfare_ai_backend
.\.venv\Scripts\python.exe ipc_main.py
```

### 但要注意

这个进程启动后会等待 **stdin JSON-RPC** 请求。  
所以它更适合：

- 在 IDE 里下断点单步调试
- 或者由 Go 自动拉起后进行联调

### 建议的 Python 重点断点位置

- `wayfare_ai_backend/ipc_main.py`
- `wayfare_ai_backend/services.py`
- `wayfare_ai_backend/database.py`
- `wayfare_ai_backend/document_parser.py`

---

## 11. 建议的 Go 重点断点位置

- `wayfare_backend/main.go`
- `wayfare_backend/ipc.go`
- `wayfare_backend/api_upload.go`
- `wayfare_backend/api_chat.go`

---

## 12. 启动后怎么做最小验证

### 12.1 先测聊天接口

```powershell
Invoke-RestMethod `
  -Method Post `
  -Uri "http://localhost:8080/chat" `
  -ContentType "application/json" `
  -Body '{"projectId":1,"context":"请简单解释一下这个项目的工作流程"}'
```

### 12.2 再测上传接口

```powershell
Invoke-RestMethod `
  -Method Post `
  -Uri "http://localhost:8080/upload" `
  -Form @{ file = Get-Item "D:\你的测试文件.pdf" }
```

> 上传接口依赖：
>
> - Python sidecar 正常启动
> - `PyMuPDF` 已安装
> - PostgreSQL 正常
> - `pgvector` 已启用
> - `LLM_API_KEY` 已配置

---

## 13. 常见报错速查

### 报错：`ModuleNotFoundError: No module named 'fitz'`

执行：

```powershell
cd D:\Desktop\Meta AI\WayFare\wayfare_ai_backend
.\.venv\Scripts\python.exe -m pip install PyMuPDF
```

### 报错：`go: downloading go1.26.1`

说明本机 Go 版本低于 `go.mod` 需要的版本。  
**优先升级 Go 到 `>= 1.26.1`。**

### 报错：`psql` 不是内部或外部命令

说明 PostgreSQL 的 `bin` 没加到 PATH。  
改 PATH，或者直接用完整路径执行 `psql.exe`。

### 报错：`extension "vector" does not exist`

说明 pgvector 没装好或没启用。  
先安装 pgvector，再执行：

```sql
CREATE EXTENSION IF NOT EXISTS vector;
```

### 报错：LLM 401 / embedding 失败 / 上传后向量入库失败

大概率是 `LLM_API_KEY` 没填。  
当前项目里这不是可选项，**要想正常上传 PDF 并做向量检索，必须填真实 key**。

### 报错：向量维度不匹配（1024 / 1536）

当前代码里数据库字段是：

```sql
vector(1024)
```

而 embedding 接口失败时，会回退成 `1536` 维零向量。  
这个问题通常不是数据库问题，而是 **API Key 没填或 embedding 请求失败**。

---

## 14. 推荐你的实际调试顺序

最省事的顺序是：

1. 先把 PostgreSQL 初始化好
2. 补 `wayfare_ai_backend/.env` 的 `LLM_API_KEY`
3. 给 Python 补 `PyMuPDF`
4. 升级 Go 到 `>= 1.26.1`
5. 在 `wayfare_backend` 目录运行 `go run .`
6. 先测 `/chat`
7. 再测 `/upload`

---

## 15. 可选：前端启动

如果后面你要一起联调前端，可以在项目根目录启动：

```powershell
cd D:\Desktop\Meta AI\WayFare
npm run dev
```

---

如果你愿意，我下一步可以继续帮你做两件事里的任意一件：

1. 直接给你补一份 **PostgreSQL 初始化 SQL 文件**
2. 继续帮你生成 **VS Code 的 Go/Python 联调 launch.json**
